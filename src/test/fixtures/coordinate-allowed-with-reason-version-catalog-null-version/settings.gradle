dependencyResolutionManagement {
  versionCatalogs {
    "libs" {
      library("exam", "com.example", "example").withoutVersion()
      library("examPlatform", "com.example", "example-platform").version("1.0.0")
    }
  }
}

include ':'
